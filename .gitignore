/node_modules
/public/hot
!/public/build
/public/storage
/public/vendor/log-viewer/
/storage/*.key
/storage/files
/storage/files/_temp_ocr
/vendor
.env
.env.dev
.env.dev.local
.env.backup*
.phpunit.result.cache
.phpunit.cache
/.phpunit.cache/
/.phpunit.result.cache
.styleci.yml
docker-compose.override.yml
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/.idea
/.vscode
/bootstrap/cache
meilisearch
home/forge/meilifiles
auth.json
# Meilisearch data
data.ms/
*.mdb
mailtrap-mcp/
.claude/settings.local.json
/storage/blaze

# Python bytecode from the CV scripts
scripts/__pycache__/
*.pyc

# EWCCV scraper debug screenshots. Written on every run, ~39MB and growing,
# and public/ is web-served — the ones already committed are fetchable at
# https://hive.contractors/EWCCV/*.png, including login-flow captures.
# Debug artifacts, never deploy input.
/public/EWCCV/

# Debug render dump of the Projects table. public/ is web-served, and this
# file contained real client names at https://hive.contractors/perf.html
# with no auth. Never commit rendered app output into public/.
/public/perf.html

# Bundled extension credentials — never commit
scripts/menards-receipt-extension/defaults.json
